#!/usr/bin/env bash

timezones=("Europe/Amsterdam" "America/New_York" "America/Los_Angeles" "UTC")

for tz in "${timezones[@]}"; do
  time=$(TZ="$tz" date "+%A %d %B %H:%M %Z")
  printf "%s %s\n" "$time" "$tz"
done
